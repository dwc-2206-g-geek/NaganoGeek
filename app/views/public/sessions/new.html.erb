<div class="container">
  <div class='row'>
    <div class="col-md-10 px-5 px-sm-0 mx-auto mt-4">
      
  <h1>ログイン</h1>

  <div class="mt-4">
    <h2>会員の方はこちらからログイン</h2>

    <%= form_with model: @customer, url: session_path(resource_name) do |f| %>

        <div class="form-row mt-5">
          <%= f.label :メールアドレス, class: 'col-sm-2 col-form-label' %>
          <div class="col">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", :placeholder => "sample@example.com", class: 'form-control exampleInputEmail1' %>
          </div>
        </div>

        <div class="form-row mt-4">
          <%= f.label :パスワード, class: 'col-sm-2 col-form-label' %>
          <div class="col">
          <%= f.password_field :password, autocomplete: "current-password", class: 'form-control exampleInputPassword1' %>
          </div>
        </div>

        <div class="mt-4 mb-4">
          <%= f.submit "ログイン", class: 'btn btn-primary' %>
        </div>
      <% end %>
  </div>

  <h2>会員登録がお済みでない方</h2>
  <p><%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください。</p>
  </div>
  </div>
</div>
