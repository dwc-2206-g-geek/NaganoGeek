<h1>ショッピングカート</h1>

<%= button_to "カートを空にする", destroy_all_cart_item_index, method: :delete %>

<%= rander "cart" cart_items: @cart_items, item: @item >

<table>
  <tr>
    <th>合計金額</th>
    <td><%= @total %>
  </tr>
</table

<%= link_to "買い物を続ける" 